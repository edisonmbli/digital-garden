# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

# 基础 URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# 系统用户 ID
AUTHOR_USER_ID=your_author_user_id_here

DATABASE_URL="postgresql://user:password@localhost:5432/database?schema=public"

# Clerk Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here

# Clerk Custom Pages
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
# NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
# NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# Clerk sign out redirect URL
NEXT_PUBLIC_CLERK_AFTER_SIGN_OUT_URL=/

# Clerk Webhook Secret
CLERK_WEBHOOK_SECRET=your_clerk_webhook_secret_here

# Sanity 项目配置（服务端专用，不再暴露给客户端）
SANITY_PROJECT_ID=your_sanity_project_id_here
SANITY_DATASET=your_sanity_dataset_here
SANITY_API_VERSION=your_sanity_api_version_here

# Sanity Editor Token
SANITY_API_EDITOR_TOKEN=your_sanity_editor_token_here

# Preview Configuration
SANITY_PREVIEW_SECRET=your_sanity_preview_secret_here
SANITY_STUDIO_PREVIEW_SECRET=your_sanity_studio_preview_secret_here

# Sanity API Token (for server-side operations)
SANITY_API_TOKEN=your_sanity_api_token_here

# Sanity Webhook Secret (for webhook verification)
SANITY_WEBHOOK_SECRET=your_sanity_webhook_secret_here

# Webhook Rate Limiting (calls per hour)
WEBHOOK_RATE_LIMIT_PER_HOUR=200

# System Configuration
NEXT_PUBLIC_SITE_NAME="Your Site Name"
NEXT_PUBLIC_CONTACT_EMAIL="your_email@example.com"
NEXT_PUBLIC_COPYRIGHT_TEXT="©Your Company"

# Sentry Configuration
# 获取方式：登录 Sentry Dashboard -> Settings -> Projects -> [Your Project] -> Client Keys (DSN)
SENTRY_DSN=your_sentry_dsn_here
NEXT_PUBLIC_SENTRY_DSN=your_public_sentry_dsn_here

# Sentry Organization & Project (用于构建时上传 source maps)
SENTRY_ORG=your_sentry_org_here
SENTRY_PROJECT=your_sentry_project_here

# 分析数据保留天数
ANALYTICS_RETENTION_DAYS=180

# cron job 密钥
CRON_SECRET=your_cron_secret_here

# cloudfare api 配置
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ZONE_ID=your_cloudflare_zone_id_here

# 站点 URL（用于生成缓存清理的完整 URL）
NEXT_PUBLIC_SITE_URL=https://yourdomain.com